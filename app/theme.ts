'use client';
import { createTheme } from '@mui/material/styles';

export const theme = createTheme({
  palette: {
    text: {
      primary: '#000000', // texto principal negro
    },
    background: {
      default: '#f5f6fa', // fondo claro opcional
    },
  },
});
